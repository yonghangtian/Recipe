spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/recipe_prod
    username: prod_user
    password: tian
    platform: mysql
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: false

server:
  port: 8081